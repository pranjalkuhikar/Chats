<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Your Chat</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div
      class="my-3 py-8 px-8 w-1/2 mx-auto bg-white rounded-xl shadow-lg space-y-2 sm:py-4 sm:items-center sm:space-x-auto"
    >
      <h1 class="text-7xl text-black font-semibold text-center py-8">
        <u>Edit Your Chat</u>
      </h1>

      <form
        class="w-full"
        method="POST"
        action="/chats/<%= chat._id %>?_method=PUT"
      >
        <div
          class="my-3 py-8 px-8 max-w-lg mx-auto bg-white rounded-xl shadow-lg space-y-2 sm:py-4 sm:items-center sm:space-x-6"
        >
          <p class="text-2xl text-black text-center py-8">
            This Chat is sent from <b><%= chat.from %></b> to
            <b><%= chat.to %></b>
          </p>
          <div class="md:items-center mb-6">
            <textarea
              class="bg-gray-200 appearance-none border-2 border-gray-200 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-green-500"
              type="text"
              name="msg"
              placeholder="Write Message"
              cols="90px"
            >
  <%= chat.msg %></textarea
            >
          </div>
          <div class="md:items-center text-center">
            <button
              class="shadow bg-green-500 hover:bg-green-400 focus:shadow-outline focus:outline-none text-white font-bold py-2 px-4 rounded"
            >
              Update
            </button>
          </div>
        </div>
      </form>
    </div>
  </body>
</html>
